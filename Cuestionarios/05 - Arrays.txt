Los elementos de un array se relacionan porque tienen el mismo 
- NOMBRE Y TIPO DE DATO


¿Qué es un array?
- UN CONJUNTO DE ELEMENTOS DEL MISMO TIPO BAJO EL MISMO NOMBRE


Indique cuál o cuáles de las siguientes formas de declarar un array es correcta:
- int miArrat[20];


¿Cuál de las siguientes afirmaciones relacionada con los arrays es correcta?
- EL INDICE COMIENZA EN 0 Y VA HASTA LA CANTIDAD DE ELEMENTOS MENOS UNO


Indique cuál de los siguientes ordenamientos es correcto:
- OPCION C
int miArray[LEN] = {7,4,9,2,1,0,18,55,21,3};
int aux, i, j;

for(i=0; i<LEN; i++)
{
	aux = miArray[i];
	j = i -1;
	while(j>=0 && aux<miArray[j])
	{
		miArray[j+1] = miArray[j];
		j--;
	}
	miArray[j+1] = aux;
}



Seleccione cuál es la declaración correcta si se desea manejar la información (número de artículo, cantidad y precio) de 500 productos de una empresa. 
- int nroArticulo[500];
  int cantidad[500];
  float precio[500];


Según el siguiente fragmento de código:

int miArray[5];
int mayor;

    for(int i=0; i<5; i++)
    {
        printf("Ingrese numero para la posicion %d: ", i);
        scanf("%d", &miArray[i]);
    }

    for(int i=0; i<5; i++)
    {
        if(miArray[i]>mayor)
        {
            mayor = miArray[i];
        }
    }
    printf("El mayor es %d", mayor);

- HAY UN ERROR EN LA DECLARACION O INICIALIZACION DE VARIABLES


Según la forma que se desee cargar el array, ¿qué estructura iterativa se recomienda usar?
- ALEATORIA: WHILE, DO WHILE
- SECUENCIAL: FOR


Al número utilizado para hacer referencia a un elemento particular de un array se lo llama:
- SUBINDICE

